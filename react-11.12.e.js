const Autumn = () => {
  return (
    <p>
      In the field where autumn winds once lingered<br />
      Evening smoke rises gently into the sky
    </p>
  );
};

const yellow = ReactDOM.createRoot(document.getElementById('root'));
yellow.render(<Autumn />);