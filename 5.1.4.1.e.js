const myApp = <h1>React Test</h1>;

// Location where the content will be rendered
const root = ReactDOM.createRoot( document.getElementById( 'root' ) ); 
root.render( myApp ); // Content to render